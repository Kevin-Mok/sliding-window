# Lesson Adjustment Log

## 2026-04-22
- Implemented an interleaved in-class lesson deck sequence for `LessonFlowDeck`, alternating teaching blocks and problem phases to create break points.
- Restructured the 2-hour class flow to keep concept teaching before use: `recognize-constraint`, `derive-state`, P1, `fixed-proof`, `pattern-decision`, P2, `variable-demo`, `invariant`, P3.
- Marked `smallest-announcement-clip` as homework-only to keep total in-class runtime at 120 minutes.
- When planning commits for dirty work in this repo, keep relevant docs in the same commit as the related implementation change instead of splitting them into a separate docs commit.
